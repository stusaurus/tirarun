name: Gameplay syntax check

on:
  push:
    paths:
      - '.github/workflows/gameplay-syntax-check.yml'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check JavaScript
        run: node --check game.js
